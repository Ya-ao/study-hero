# 学习英雄 - 后端开发计划

## 一、开发概述

### 1.1 开发目标

基于产品需求文档和技术架构文档，完成后端系统所有核心功能模块的开发与测试。

### 1.2 技术栈

* **运行环境**: Node.js 18+

* **Web框架**: Express

* **数据库**: Supabase (PostgreSQL)

* **AI服务**: 豆包 API

### 1.3 开发原则

* 严格遵循技术架构文档规范

* 代码结构清晰，便于维护

* 完善的错误处理机制

* 统一的响应格式

***

## 二、开发任务分解

### 阶段一：项目初始化 (Day 1)

#### 任务1.1 创建项目结构

```
server/
├── src/
│   ├── controllers/        # 控制器
│   ├── models/             # 数据模型
│   ├── routes/             # 路由定义
│   ├── services/           # 业务逻辑
│   ├── middlewares/        # 中间件
│   └── utils/              # 工具函数
├── config/
│   └── index.js            # 配置文件
├── tests/                  # 测试文件
├── app.js                  # 应用入口
├── package.json
└── .env                    # 环境变量
```

#### 任务1.2 安装依赖

* express: Web框架

* @supabase/supabase-js: Supabase客户端

* dotenv: 环境变量管理

* cors: 跨域处理

* jest: 单元测试

#### 任务1.3 配置数据库

* 创建Supabase项目

* 配置数据库连接

* 创建数据表结构

***

### 阶段二：基础模块开发 (Day 2)

#### 任务2.1 统一响应格式

```javascript
// 成功响应
{ code: 0, data: {}, message: 'success' }

// 错误响应
{ code: -1, data: null, message: '错误信息' }
```

#### 任务2.2 认证中间件

* 微信登录凭证验证

* 用户身份识别

* Token解析

#### 任务2.3 工具函数

* 日期处理

* 数据格式化

* 错误处理

***

### 阶段三：用户模块开发 (Day 3)

#### 任务3.1 用户模型

```javascript
// models/User.js
- findById(id)
- findByOpenid(openid)
- create(data)
- update(id, data)
- updateStats(id, stats)
```

#### 任务3.2 用户接口

| 接口     | 方法   | 路径              | 说明             |
| ------ | ---- | --------------- | -------------- |
| 微信登录   | POST | /api/user/login | 微信code换取openid |
| 获取用户信息 | GET  | /api/user/info  | 获取当前用户信息       |
| 更新用户信息 | PUT  | /api/user/info  | 更新昵称头像         |

#### 任务3.3 用户控制器

```javascript
// controllers/user.js
- login()      // 微信登录
- getInfo()    // 获取用户信息
- updateInfo() // 更新用户信息
```

***

### 阶段四：答题模块开发 (Day 4-5)

#### 任务4.1 学习记录模型

```javascript
// models/Record.js
- create(data)
- findByUserId(userId, options)
- findRecent(userId, limit)
- findToday(userId)
```

#### 任务4.2 AI服务集成

```javascript
// services/ai.js
- generateQuestions(topic, difficulty, count)
- buildPrompt(topic, difficulty, count)
- parseResponse(response)
```

#### 任务4.3 答题接口

| 接口   | 方法   | 路径                  | 说明     |
| ---- | ---- | ------------------- | ------ |
| 生成问题 | POST | /api/game/questions | AI生成题目 |
| 提交结果 | POST | /api/game/submit    | 提交答题结果 |

#### 任务4.4 答题控制器

```javascript
// controllers/game.js
- generateQuestions()  // 生成问题
- submit()            // 提交答题结果
```

***

### 阶段五：统计模块开发 (Day 6)

#### 任务5.1 统计接口

| 接口   | 方法  | 路径                      | 说明     |
| ---- | --- | ----------------------- | ------ |
| 今日统计 | GET | /api/stats/today        | 今日答题数据 |
| 最近学习 | GET | /api/stats/recent       | 最近学习记录 |
| 学习历史 | GET | /api/stats/history      | 完整学习历史 |
| 成就列表 | GET | /api/stats/achievements | 用户成就列表 |
| 热门主题 | GET | /api/topics/hot         | 热门主题列表 |

#### 任务5.2 统计控制器

```javascript
// controllers/stats.js
- getToday()        // 今日统计
- getRecent()       // 最近学习
- getHistory()      // 学习历史
- getAchievements() // 成就列表
```

#### 任务5.3 热门主题控制器

```javascript
// controllers/topic.js
- getHotTopics()    // 获取热门主题
```

***

### 阶段六：成就系统开发 (Day 7)

#### 任务6.1 成就模型

```javascript
// models/Achievement.js
- create(data)
- findByUserId(userId)
- checkAndUnlock(userId, condition)
```

#### 任务6.2 成就服务

```javascript
// services/achievement.js
- checkFirstAnswer(user)      // 首次答题
- checkAnswerCount(user)      // 累计答题
- checkStreakDays(user)       // 连续天数
- checkPerfectScore(record)   // 满分
- checkAccuracy(record)       // 正确率
- checkAll(user, record)      // 检查所有成就
```

#### 任务6.3 成就规则实现

| 成就类型          | 检查条件                   |
| ------------- | ---------------------- |
| first\_answer | totalQuestions === 0   |
| answer\_100   | totalQuestions >= 100  |
| answer\_500   | totalQuestions >= 500  |
| answer\_1000  | totalQuestions >= 1000 |
| streak\_7     | streakDays >= 7        |
| streak\_30    | streakDays >= 30       |
| perfect       | score === 100          |
| accuracy\_90  | correctRate >= 0.9     |

***

### 阶段七：测试与优化 (Day 8-9)

#### 任务7.1 单元测试

* [ ] 用户模块测试

* [ ] 答题模块测试

* [ ] 统计模块测试

* [ ] 成就系统测试

* [ ] AI服务测试

#### 任务7.2 接口测试

* [ ] 用户接口测试

* [ ] 答题接口测试

* [ ] 统计接口测试

* [ ] 热门主题接口测试

#### 任务7.3 集成测试

* [ ] 完整答题流程测试

* [ ] 用户登录流程测试

* [ ] 成就解锁流程测试

#### 任务7.4 性能优化

* [ ] 数据库查询优化

* [ ] AI接口响应优化

* [ ] 接口响应时间优化

***

## 三、数据库表结构

### 3.1 用户表

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  openid VARCHAR(64) UNIQUE NOT NULL,
  nick_name VARCHAR(64),
  avatar_url VARCHAR(512),
  total_questions INTEGER DEFAULT 0,
  correct_count INTEGER DEFAULT 0,
  streak_days INTEGER DEFAULT 0,
  last_study_date DATE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_users_openid ON users(openid);
```

### 3.2 学习记录表

```sql
CREATE TABLE study_records (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  topic VARCHAR(128) NOT NULL,
  difficulty VARCHAR(16) NOT NULL,
  total_questions INTEGER NOT NULL,
  correct_count INTEGER NOT NULL,
  score INTEGER NOT NULL,
  duration INTEGER NOT NULL,
  questions JSONB NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_study_records_user_id ON study_records(user_id);
CREATE INDEX idx_study_records_created_at ON study_records(created_at DESC);
```

### 3.3 成就表

```sql
CREATE TABLE achievements (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  type VARCHAR(32) NOT NULL,
  name VARCHAR(64) NOT NULL,
  icon VARCHAR(16) NOT NULL,
  unlocked_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, type)
);

CREATE INDEX idx_achievements_user_id ON achievements(user_id);
```

***

## 四、API接口清单

### 4.1 用户模块

| 接口     | 方法   | 路径              | 说明       |
| ------ | ---- | --------------- | -------- |
| 微信登录   | POST | /api/user/login | 微信code登录 |
| 获取用户信息 | GET  | /api/user/info  | 获取用户信息   |
| 更新用户信息 | PUT  | /api/user/info  | 更新昵称头像   |

### 4.2 答题模块

| 接口   | 方法   | 路径                  | 说明     |
| ---- | ---- | ------------------- | ------ |
| 生成问题 | POST | /api/game/questions | AI生成题目 |
| 提交结果 | POST | /api/game/submit    | 提交答题结果 |

### 4.3 统计模块

| 接口   | 方法  | 路径                      | 说明     |
| ---- | --- | ----------------------- | ------ |
| 今日统计 | GET | /api/stats/today        | 今日答题数据 |
| 最近学习 | GET | /api/stats/recent       | 最近学习记录 |
| 学习历史 | GET | /api/stats/history      | 完整学习历史 |
| 成就列表 | GET | /api/stats/achievements | 用户成就列表 |

### 4.4 主题模块

| 接口   | 方法  | 路径              | 说明     |
| ---- | --- | --------------- | ------ |
| 热门主题 | GET | /api/topics/hot | 热门主题列表 |

***

## 五、开发规范

### 5.1 代码规范

* 使用ES6+语法

* 统一使用const/let

* 异步操作使用async/await

* 函数添加必要注释

### 5.2 命名规范

* 文件名：小写中划线 (user-controller.js)

* 类名：大驼峰 (UserController)

* 函数名：小驼峰 (getUserInfo)

* 常量：大写下划线 (API\_BASE\_URL)

### 5.3 错误处理

* 统一错误码定义

* 友好错误提示

* 错误日志记录

### 5.4 安全规范

* 参数校验

* SQL注入防护

* XSS防护

* 敏感信息加密

***

## 六、测试计划

### 6.1 测试范围

* 单元测试：所有模块函数

* 接口测试：所有API接口

* 集成测试：核心业务流程

### 6.2 测试工具

* Jest：单元测试框架

* Supertest：接口测试

### 6.3 测试覆盖率

* 目标覆盖率：80%以上

* 核心模块覆盖率：90%以上

***

## 七、交付物

1. **源代码**

   * 完整的后端项目代码

   * 测试代码

2. **文档**

   * API接口文档

   * 数据库设计文档

   * 部署说明文档

3. **测试报告**

   * 单元测试报告

   * 接口测试报告

   * 性能测试报告

