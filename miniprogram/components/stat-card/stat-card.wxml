<view class="stat-card">
  <view class="stat-icon" wx:if="{{icon}}">{{icon}}</view>
  <view class="stat-value">{{value}}<text class="stat-unit" wx:if="{{unit}}">{{unit}}</text></view>
  <view class="stat-title">{{title}}</view>
</view>
